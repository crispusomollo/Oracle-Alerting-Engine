def notify(severity, alert_id, value, incident_id):
    # Stub for Slack / Email / PagerDuty
    print(f"[{severity}] {alert_id} triggered (value={value})")
    print(f"Incident created: {incident_id}")
def notify(severity, alert_id, value, incident_id):
    # Stub for Slack / Email / PagerDuty
    print(f"[{severity}] {alert_id} triggered (value={value})")
    print(f"Incident created: {incident_id}")

